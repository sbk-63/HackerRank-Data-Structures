    public static List<Integer> rotateLeft(int d, List<Integer> arr) {
        for(int i = 0; i < d; i++){
            int n = arr.remove(0);
            arr.add(arr.size(), n);
        }
        return arr;
    }
